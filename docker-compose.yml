version: "3.9"
services:
  redis:
    image: redis:alpine
    container_name: redis
    restart: always
    ports:
      - "6379:6379"
    networks:
      - app_network
  # zookeeper:
  #   image: confluentinc/cp-zookeeper:7.4.1
  #   container_name: zookeeper
  #   environment:
  #     ZOOKEEPER_CLIENT_PORT: 2181
  #   ports:
  #     - "2181:2181"
  #   networks:
  #     - app_network
  # kafka:
  #   image: confluentinc/cp-kafka:7.4.4
  #   container_name: kafka
  #   depends_on:
  #     - zookeeper
  #   environment:
  #     KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
  #     KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092 # Dành cho các container khác
  #     KAFKA_LISTENER_SECURITY_PROTOCOL: PLAINTEXT
  #     KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092 # Chấp nhận kết nối từ tất cả địa chỉ
  #     KAFKA_LISTENER_PORT: 9092
  #     KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
  #   ports:
  #     - "9092:9092"
  #   command:
  #     - sh
  #     - -c
  #     - |
  #       sleep 5;
  #       /etc/confluent/docker/run &
  #       # Tạo các topic cho các sự kiện
  #       kafka-topics --create --topic shipping-update-quantity --bootstrap-server kafka:9092 --replication-factor 1 --partitions 3;
  #       kafka-topics --create --topic shipping-create-shipment --bootstrap-server kafka:9092 --replication-factor 1 --partitions 3;
  #       kafka-topics --create --topic shipping-get-shipment-status --bootstrap-server kafka:9092 --replication-factor 1 --partitions 3;
  #       kafka-topics --create --topic inventory-update-quantity --bootstrap-server kafka:9092 --replication-factor 1 --partitions 3;
  #       kafka-topics --create --topic inventory-get-quantity --bootstrap-server kafka:9092 --replication-factor 1 --partitions 3;
  #       kafka-topics --create --topic inventory-inventory-alert --bootstrap-server kafka:9092 --replication-factor 1 --partitions 3;
  #       kafka-topics --list --bootstrap-server kafka:9092;
  #       wait
  #   networks:
  #     - app_network

  api-gateway:
    build: ./api-gateway
    container_name: api-gateway
    environment:
      USER_SERVICE_URL: http://user:1111
      PRODUCT_SERVICE_URL: http://product:4444
      INVENTORY_SERVICE_URL: https://inventory.qcapsule.io.vn
      CART_SERVICE_URL: http://cart:4445
      ORDER_SERVICE_URL: http://order:3333
      PAYMENT_SERVICE_URL: http://payment:3334
      SHIPPING_SERVICE_URL: https://shipping.qcapsule.io.vn
      REVIEW_RATING_SERVICE_URL: http://reviewrating:5555
      NOTIFICATION_SERVICE_URL: http://notification:5556
      RECOMMENDATION_SERVICE_URL: http://recommendation:1112
    depends_on:
      # - kafka
      - user
      - order
      - payment
      - product
      - cart
      # - redis
      - rating
      # - notification
      - recommendation
    ports:
      - "6868:6868"
    networks:
      - app_network

  user:
    build: ./userService
    container_name: user
    environment:
      DB_NAME: chptt_gear_ot8g
      DB_USER: sa
      DB_PASS: Gyb6QZCn9AqnbP8kLceq6du0fJGpOfeT
      DB_HOST: dpg-cv3vqo9u0jms73ee72hg-a.singapore-postgres.render.com
      DB_PORT: 5432
      CLIENT_PORT: 3000
      PORT: 1111
      ACCESS_TOKEN_SECRET: chptt@20075851
      CLOUDINARY_CLOUD_NAME: dsgm7mp23
      CLOUDINARY_API_KEY: 687534721666436
      CLOUDINARY_API_SECRET: Rf7RBNDCGZFw_B1Tvsv3odTCMhg
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      # - kafka
      - redis
    ports:
      - "1111:1111"
    networks:
      - app_network

  recommendation:
    build: ./recommendationService
    container_name: recommendation
    environment:
      PORT: 1112
      GEMINI_API_KEY: AIzaSyCHrFVJlM8a6MqJmwsm_AaplPFPdKBATyw
      PROJECT_ID: gen-lang-client-0094505557
      LOCATION: asia-southeast1
      GOOGLE_APPLICATION_CREDENTIALS: ./config/gcp-key.json

    # depends_on:
    # - kafka
    # - redis
    ports:
      - "1112:1112"
    networks:
      - app_network

  order:
    build: ./orderService
    container_name: order
    environment:
      DB_NAME: mixed_chptt_lth
      DB_USER: sa
      DB_PASS: QuCo6aVMvCNfDwcBdD7Be2OJka0rLFiZ
      DB_HOST: dpg-cuoucjlds78s738ol82g-a.singapore-postgres.render.com
      DB_PORT: 5432
      DB_DIALECT: postgres
      PORT: 3333
      KAFKA_BROKER: kafka:9092
      CLIENT_HOST: http://localhost:3000
      CLIENT_ID: orderService
      GROUP_ID: order-group
    # depends_on:
    #   - kafka
    ports:
      - "3333:3333"
    networks:
      - app_network

  payment:
    build: ./paymentService
    container_name: payment
    environment:
      DB_NAME: mixed_chptt_lth
      DB_USER: sa
      DB_PASS: QuCo6aVMvCNfDwcBdD7Be2OJka0rLFiZ
      DB_HOST: dpg-cuoucjlds78s738ol82g-a.singapore-postgres.render.com
      DB_PORT: 5432
      DB_DIALECT: postgres
      PORT: 3334
      KAFKA_BROKER: kafka:9092
      CLIENT_HOST: http://localhost:3000
      CLIENT_ID: payment
      GROUP_ID: payment-group
    # depends_on:
    #   - kafka
    ports:
      - "3334:3334"
    networks:
      - app_network

  product:
    build:
      context: ./productService # Thư mục chứa ứng dụng Java
      dockerfile: Dockerfile # Tệp Dockerfile
    container_name: product
    # depends_on:
    #   - kafka # Kafka cần chạy trước
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://dpg-cv3vqo9u0jms73ee72hg-a.singapore-postgres.render.com/chptt_gear_ot8g
      SPRING_DATASOURCE_USERNAME: sa
      SPRING_DATASOURCE_PASSWORD: Gyb6QZCn9AqnbP8kLceq6du0fJGpOfeT
      SPRING_APPLICATION_NAME: productService
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_JPA_SHOW_SQL: "true"
      SERVER_PORT: 4444 # Đảm bảo rằng server chạy đúng cổng
    ports:
      - "4444:4444" # Mở cổng 4444
    networks:
      - app_network # Mạng tùy chỉnh để giao tiếp giữa các container

  cart:
    build:
      context: ./cartService # Thư mục chứa ứng dụng Java
      dockerfile: Dockerfile # Tệp Dockerfile
    container_name: cart
    # depends_on:
    #   - kafka # Kafka cần chạy trước
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://dpg-cv3vqo9u0jms73ee72hg-a.singapore-postgres.render.com/chptt_gear_ot8g
      SPRING_DATASOURCE_USERNAME: sa
      SPRING_DATASOURCE_PASSWORD: Gyb6QZCn9AqnbP8kLceq6du0fJGpOfeT
      SPRING_APPLICATION_NAME: cartService
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_JPA_SHOW_SQL: "true"
      SERVER_PORT: 4445 # Đảm bảo rằng server chạy đúng cổng
    ports:
      - "4445:4445" # Mở cổng 4444
    networks:
      - app_network # Mạng tùy chỉnh để giao tiếp giữa các container

  rating:
    build:
      context: ./ratingService # Thư mục chứa ứng dụng Java
      dockerfile: Dockerfile # Tệp Dockerfile
    container_name: rating
    # depends_on:
    #   - kafka # Kafka cần chạy trước
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://dpg-cv3vqo9u0jms73ee72hg-a.singapore-postgres.render.com/chptt_gear_ot8g
      SPRING_DATASOURCE_USERNAME: sa
      SPRING_DATASOURCE_PASSWORD: Gyb6QZCn9AqnbP8kLceq6du0fJGpOfeT
      SPRING_APPLICATION_NAME: ratingService
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_JPA_SHOW_SQL: "true"
      SERVER_PORT: 5555 # Đảm bảo rằng server chạy đúng cổng
    ports:
      - "5555:5555"
    networks:
      - app_network # Mạng tùy chỉnh để giao tiếp giữa các container

networks:
  app_network:
    driver: bridge
